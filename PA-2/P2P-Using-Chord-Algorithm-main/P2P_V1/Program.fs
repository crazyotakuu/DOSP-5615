// Learn more about F# at http://docs.microsoft.com/dotnet/fsharp

open System
open P2P

let no_of_peers = int(Environment.GetCommandLineArgs().[1])
let no_of_req = int(Environment.GetCommandLineArgs().[2])